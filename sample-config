# save this at $HOME/.interhack/config

# main config
nick Eidolos

# keymaps
# map X doesn't work, e.g. DX
#map X \#enhance\n

# output remappings
s/You hear someone counting money\./There is a vault on this level! /g
s/You hear the footsteps of a guard on patrol\./\
There is definitely a vault on this level!    /g

eval $_ =~ /^A/ ? ("A" x length) : $_

# simple colorings
color red the high priest(ess)? of (?!Moloch)[a-zA-Z- ]+

color yellow \d+ gold pieces?\.

